# Files and Data

18 November 2025

## Files
how to read and open text file
when open, we need to consider the location
or **path** to the file.
The **path** to the file is the direction. we take to get to the file
Included inn a path, is the **file's name** and the folder that it lives in

### Opening Files
To open a file, we use the `open()` function. the `open()` funcytion returns a **file object**. A **file object** represents a **file stream** which is lie a pipe that connects our python file to the external file.

### Reading their contents

### Managing File Streams
to lower the risk of corrupt any data, we use `.close()` to close a file stream

## Lists